#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - The number is positive or negative
 * Return: Void
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if (n > 0);
{
        putchar('is positive.');
        else if (n < 0);
{
	putchar ('is negative');
        else (n = 0);
{
	putchar('is zero');
        putchar('\n');

	return (0);
}
}
}
}
